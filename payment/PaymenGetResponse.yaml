# ~PaymentGetResponse ----------------------------

  PaymentGetResponse:
      type: object
      properties:
          payments:
              type: array
              items:
                $ref: '#/definitions/PaymentInfo'
      xml:
          name: PaymentGetResponse